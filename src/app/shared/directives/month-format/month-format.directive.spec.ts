import { MonthFormatDirective } from './month-format.directive';

describe('YearFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new MonthFormatDirective();
    expect(directive).toBeTruthy();
  });
});
